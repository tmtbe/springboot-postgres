spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${embedded.postgresql.host}:${embedded.postgresql.port}/example
    username: ${embedded.postgresql.user}
    password: ${embedded.postgresql.password}